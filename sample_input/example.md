# Payments Service

The payments service handles charges and refunds.

## Configuration

Set the following environment variables before starting.

### API Keys

Store the secret key in `STRIPE_SECRET_KEY`. Never commit it to git.

## Deployment

Deploy with Docker.

### Kubernetes

Use the provided Helm chart for production clusters.
